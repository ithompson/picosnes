pub fn nop(_cpu: &mut Cpu6502, _val: &mut u8) {
    // No operation
}

{% for mnemonic, mnemonic_data in cpu_data.mnemonics.items() %}
    {% if mnemonic_data.op or mnemonic_data.branch_cond %}
pub fn {{ mnemonic|lower }}(cpu: &mut Cpu6502, val: &mut u8) {
        {% if mnemonic_data.op %}
    // @pseudocode: {{ mnemonic_data.op }}
        {% elif mnemonic_data.branch_cond %}
    // @pseudocode: {reg} = {{ mnemonic_data.branch_cond }}
        {% endif %}
        {% if mnemonic_data.flags %}
    // @flags: {{ mnemonic_data.flags }}
        {% endif %}
    todo!("Mnemonic {{ mnemonic|upper }}");
}
    {% endif %}
{% endfor %}